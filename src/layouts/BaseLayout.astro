---
import '../styles/global.css';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}/favicon.svg`} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="A personal digital garden - bookmarks, videos, papers, and writings." />
    <title>{title}</title>
  </head>
  <body>
    <nav class="nav">
      <a href={`${import.meta.env.BASE_URL}/`} class="nav-brand">synechism</a>
      <ul class="nav-links">
        <li><a href={`${import.meta.env.BASE_URL}/writings`}>Writings</a></li>
        <li><a href={`${import.meta.env.BASE_URL}/resources`}>Resources</a></li>
        <li><a href={`${import.meta.env.BASE_URL}/papers`}>Papers</a></li>
        <li><a href={`${import.meta.env.BASE_URL}/now`}>Now</a></li>
        <li><a href={`${import.meta.env.BASE_URL}/about`}>About</a></li>
      </ul>
    </nav>

    <main>
      <slot />
    </main>

    <button class="theme-toggle" id="theme-toggle">Dracula</button>

    <script>
      // Theme toggle placeholder - can be expanded later
      const toggle = document.getElementById('theme-toggle');
      toggle?.addEventListener('click', () => {
        // Future: toggle between color schemes
      });
    </script>
  </body>
</html>
